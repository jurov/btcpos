Development version. Run electrum client >= 2.4 (watch-only wallet from public key can be used) with jsonrpc GUI:

electrum -g jsonrpc

The POS will communicate with electrum via localhost, port 7777.
